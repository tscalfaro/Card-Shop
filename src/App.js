import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>Welcome to the Card Shop, Find every collectable card here!</p>
        <button onClick={() => alert('Button Clicked!')}>Shop Now</button>
      </header>
    </div>
  );
}

export default App;
